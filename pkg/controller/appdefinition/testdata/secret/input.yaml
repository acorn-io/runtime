kind: AppInstance
apiVersion: acorn.io/v1
metadata:
  uid: 01234567890abcdef
  name: app-name
  namespace: app-namespace
spec:
  image: test
  publishAllPorts: true
  endpoints:
  - target: oneimage
    hostname: localhost
status:
  namespace: app-created-namespace
  appSpec:
    containers:
      oneimage:
        image: "image-name"
        dirs:
          noaction:
            secret:
              name: secret_dir_noaction
              onChange: noAction
          redeploy:
            secret:
              name: secret_dir_redploy
              onChange: redeploy
          missing:
            secret:
              name: secret_dir_missing
              onChange: redeploy
        files:
          noaction:
            secret:
              name: secret_file_noaction
              key: key
              onChange: noAction
          redeploy:
            secret:
              name: secret_file_redeploy
              key: key
              onChange: redeploy
          missing:
            secret:
              name: secret_file_missing
              key: key
              onChange: redeploy
        environment:
        - name: NOACTION
          secret:
            name: secret_env_noaction
            key: key
            onChange: noAction
        - name: REDEPLOY
          secret:
            name: secret_env_redploy
            key: key
            onChange: redeploy
        - name: MISSING
          secret:
            name: secret_env_missing
            key: key
            onChange: redeploy
